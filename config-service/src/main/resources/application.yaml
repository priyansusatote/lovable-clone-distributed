spring:
  application:
    name: config-service

  cloud:
    config.server.git:
      uri: https://github.com/priyansusatote/lovable-config-server
      username: priyansusatote
      password: ${GITHUB_TOKEN}
      default-label: main

server.port: 8888
